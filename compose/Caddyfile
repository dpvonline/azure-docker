{
	email {$LETSENCRYPT_EMAIL}
}

{$DOMAIN_AUTH} {
	reverse_proxy keycloak:8080
}

# `tls internal` until Cutover A — REMOVE IT THEN. Public DNS for
# wiki.dpvonline.de still points at AKS, so Let's Encrypt cannot validate this
# host yet. Until then the test copy is reached through an /etc/hosts entry
# pointing at this VM, and the browser warns about Caddy's own certificate.
wiki.dpvonline.de {
	tls internal
	reverse_proxy confluence:8090
}
